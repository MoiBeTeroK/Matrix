#include <iostream>
using namespace std;

void print(float** a, int n) {
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            cout << a[i][j] << "\t";
        }
        cout << endl;
    }
}
float diag(float **a, int n) {
    float k = 0;
    for (int i = 0; i < n; i++) {
        k += a[i][i];
    }
    return k;
}
float **sum(float** a, float** b, int n) {
    float** c;
    c = new float* [n];
    for (int i = 0; i < n; i++) {
        c[i] = new float[n];
    }
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            c[i][j] = a[i][j] + b[i][j];
        }
    }
    return c;
}
float **mul(float** a, float** b, int n) {
    float** c;
    c = new float* [n];
    for (int i = 0; i < n; i++) {
        c[i] = new float[n];
    }
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            c[i][j] = 0;
            for (int k = 0; k < n; k++)
                c[i][j] += a[i][k] * b[k][j];
        }
    }
    return c;
}
float *f(float** a, float*b,int n, int id) {
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            b[id] = a[i][j];
            id++;
        }
    }
    return b;
}

void main() {
    int n; cin >> n;
    float** a;
    a = new float* [n];
    for (int i = 0; i < n; i++) {
        a[i] = new float[n];
    }
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            cin >> a[i][j];
        }
    }
    int id=0;
    float* b = new float[n * n];
    print(a, n);
    for (int i = 0; i < n; i++) {
        delete a[i];
    }
    delete[]a;
    delete[]b;
}
